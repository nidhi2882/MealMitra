{
  "name": "mealmitra",
  "version": "1.0.0",
  "description": "MealMitra - Food Waste Management & Donation System (MERN Stack)",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
